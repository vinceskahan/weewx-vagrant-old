
# this is a 'do nothing installer' that you can specify
# in the Vagrantfile in order to do pre-weewx setup (only)
# for your selected os and version.
#
# (mainly because specifying a weewx installer is 'mandatory'
#   as the current Vagrantfile is written)
#

